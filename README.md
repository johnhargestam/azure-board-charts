# Azure Board Charts

## Development

- Run the database from docker-compose.yml
- Create an .env file with these values
  ```env
  DB_URI=localhost
  DB_USER=sa
  DB_PASSWORD=Password1
  ```
- Run `yarn serve`
